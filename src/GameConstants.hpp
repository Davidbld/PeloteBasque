#ifndef GAME_CONSTANTS_HPP
#define GAME_CONSTANTS_HPP

#include <string>

// Format de version : ReleaseVersion.BuildVersion.FixVersion
const std::string GAME_VERSION = "v0.0.1";

// Marges de sécurité pour la zone de jeu
const float MARGIN_X = 20.f;     // Marge horizontale
const float MARGIN_Y = 20.f;     // Marge verticale
const float MARGIN_TEXT = 10.f;  // Marge pour le texte

#endif // GAME_CONSTANTS_HPP